\documentclass[colorised, sobre]{template} % Options : devie, sobre, colorised

%*  List of the autors, for the cover page
\def\listAuteurs
    {\gdbdg} % authors, with \auteur

%*  List of the supervisors, for the cover page
\def\listEncadrants
    {\auteur{Prenom Encadrant}{nom Encadrant}} % Supervisor, with \auteur

%* Defines the part with the authors and framers of the cover page, used by \miniTitlePage and \titlePage
\def\enteteAuteurs
    {\auteurs
        {\listAuteurs} % list of the authors
        {\listEncadrants} % list of the supervisors
        []  % 1 if supervisors, 0 else (default 0)
        []  % 1 if only one author, 0 else (default 0)
        []} % 1 if full page, 0 if mini page, (def 0) 

%* Header
\header
    [images/Logo_ENSIMAG.png]% image left below
    {titre} % Middle title (can't stay empty)
    {de Bigault de Granrut} % last name (l1 right)
    {Guillaume} % first name (l2 right)

\begin{document}
%* Cover page
%  \miniTitlePage
\titlePage
    {images/Logo_ENSIMAG.png} % cover image, not used by miniTitlePage
    {\white{f}} % Filière
    {\white{t}} % Main title
    {\white{s}} % Subtitle
    {\listAuteurs} %(que pour grande page de garde)
    [1] % 1 si sommaire, 0 sinon (def 1)
    [0] % 1 si liste des figures, 0 sinon (def 0)
    [0] % 1 si liste des tableaux , 0 sinon (def 0)

%TODO The document 
\end{document}